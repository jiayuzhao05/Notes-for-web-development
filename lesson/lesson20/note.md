持久性闭包 vs 短暂闭包

reducer 函数编程
ts 在原生数组  
useReducer hooks
redux：reducer 独立包

展示业务层 不获取 user
登录页:zhangsan 红
详情页: zhangsan 蓝

redux：reducer 独立包
store 变化反映到 view

内存爆掉

挂载在 app 上

三个文件 userProfile1.js userProfile2.js common.js(withUser())

下周:架构 分层 布局

作业:独立完成一个登录注册页面 至少有三页 部署上线
